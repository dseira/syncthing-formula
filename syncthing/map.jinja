# -*- coding: utf-8 -*-
# vim: ft=jinja

{% set os_family_map = salt['grains.filter_by']({
    'Debian': {
    },
    'RedHat': {
    },
}, merge=salt['pillar.get']('syncthing:lookup')) %}

{% set syncthing_settings = salt['pillar.get'](
        'syncthing',
        default=os_family_map,
        merge=True
    )
%}
